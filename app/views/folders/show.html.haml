%h2
  = @folder.name
%table
  %thead
    %tr
      %th Name
      %th Image
      %th{colspan: "3"}
  %tbody
    - @documents.each do |document|
      %tr
        %td= document.name
        %td= image_tag content_type_icone(document.file), size: '25x25'
        %td= link_to 'Show', document
        %td= link_to 'Edit', edit_document_path(document)
        %td= link_to 'Delete', document, method: :delete
%br
= link_to 'Add document', new_document_path(:folder => @folder.id)
|
= link_to 'Edit', edit_folder_path(@folder)
|
= link_to 'Back', folders_path
