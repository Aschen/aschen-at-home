%h1 Listing seasons

%table
  %thead
    %tr
      %th Number
      %th Episodes count
      %th Auto download
      %th Series
      %th
      %th
      %th

  %tbody
    - @seasons.each do |season|
      %tr
        %td= season.number
        %td= season.episodes_count
        %td= season.auto_download
        %td= season.series_id
        %td= link_to 'Show', season
        %td= link_to 'Edit', edit_season_path(season)
        %td= link_to 'Destroy', season, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Season', new_season_path
